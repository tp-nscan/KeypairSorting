using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Sorting.KeyPairs;
using Sorting.Sorters;
using Sorting.Switchables;

namespace SorterEvo
{
    public static class GenomeToSwitchableGroup
    {
        //public static ISwitchableGroup<uint> ToSwitchableGroup(this IGenome<uint> genome, int keyCount)
        //{
        //    //foreach (var VARIABLE in enumerable)
        //    //{
                
        //    //}
        //    return genome.Chromosomes[0].Genes.ToS


        //    return KeyPairRepository.KeyPairSet(keyCount).KeyPairs.ToSorter
        //        (
        //            keyPairChoices: chromosome.Genes,
        //            keyCount: keyCount,
        //            guid: genome.Guid
        //        );
        //}

        //public static ISwitchableGroup<T> ToSwitchableGroup<T>(this IGenome<T> genome, int keyCount)
        //{
        //    var chromosome = genome.Chromosomes[0];


        //    return KeyPairRepository.KeyPairSet(keyCount).KeyPairs.ToSorter
        //        (
        //            keyPairChoices: chromosome.Genes,
        //            keyCount: keyCount,
        //            guid: genome.Guid
        //        );
        //}

    }
}
