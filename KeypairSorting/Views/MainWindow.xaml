<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:KeypairSorting="clr-namespace:KeypairSorting"
        xmlns:views="clr-namespace:KeypairSorting.Views"
        xmlns:viewModels="clr-namespace:KeypairSorting.ViewModels"
        x:Class="KeypairSorting.Views.MainWindow"
        Title="MainWindow" Height="350" Width="525">

    <Window.Resources>

    </Window.Resources>

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition/>
		</Grid.RowDefinitions>

		<TextBlock Text="{Binding Mode}" HorizontalAlignment="Center"/>

        <!--<views:MakeRandomSorterControl DataContext="{Binding MakeRandomSorterVm}" Grid.Row="0" Grid.RowSpan="2"/>-->

        <!--<views:MakeSorterEvalsControl  DataContext="{Binding MakeSorterEvalsVm}" Grid.Row="0" Grid.RowSpan="2"/>-->

        <!--<views:MakeSwitchableGroupsControl  DataContext="{Binding MakeSwitchableGroupsVm}" Grid.Row="0" Grid.RowSpan="2"/>-->

        <views:MakeSorterOnSwitchableGroupsControl DataContext="{Binding MakeSorterOnSwitchableGroupVms}" Grid.Row="0" Grid.RowSpan="2"/>
        
    </Grid>
</Window>
